# Vibes Stack Environment Configuration
# ======================================
# Copy this to .env and fill in your values

# ===========================================
# Domain Configuration
# ===========================================
# Your domain (e.g., vibes.yourdomain.com)
DOMAIN=localhost

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=your-email@example.com

# ===========================================
# Projects Directory
# ===========================================
# Where your git projects live on the host
PROJECTS_DIR=/home/vibes/projects

# ===========================================
# Database (PostgreSQL)
# ===========================================
POSTGRES_USER=vibes
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=vibes_kanban

# ===========================================
# Vibes Configuration
# ===========================================
# Enable git-backed Beads (Gastown mode)
VIBES_USE_BEADS=true

# ===========================================
# Claude Authentication
# ===========================================
# Option 1: Claude Pro (OAuth) - Interactive sessions only
# Just run `claude` and authenticate via browser
# No API key needed for interactive use

# Option 2: Anthropic API - For headless/automated agents (Polecats)
# Get from: https://console.anthropic.com/api-keys
# Only needed if using Fly.io burst capacity
ANTHROPIC_API_KEY=

# OpenAI API key (for PageIndex, if used)
OPENAI_API_KEY=

# ===========================================
# Ollama (Optional - Local LLM)
# ===========================================
# Username for Ollama web access
OLLAMA_USER=admin
# Generate with: caddy hash-password --plaintext 'your-password'
OLLAMA_HASH=

# ===========================================
# Security
# ===========================================
# Secret key for session encryption
SECRET_KEY=generate-a-random-string-here

# Frontend authentication (HTTP Basic Auth)
VIBES_USERNAME=admin
VIBES_PASSWORD=change-this-password

# ===========================================
# Fly.io Burst Capacity (Optional)
# ===========================================
# API token from: fly tokens create
FLY_API_TOKEN=

# App name for Polecats (create with: fly apps create vibes-polecats)
FLY_APP_NAME=vibes-polecats

# Region for Polecats (iad=US-East, lhr=London, syd=Sydney)
FLY_REGION=iad

# ===========================================
# Tailscale (Optional)
# ===========================================
# Tailscale auth key for secure access
# TAILSCALE_AUTHKEY=tskey-auth-...
